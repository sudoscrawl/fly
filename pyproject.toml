[project]
name = "fly"
version = "0.1.0"
description = "Your project's second memory"
readme = "README.md"
authors = [
    { name = "Ayush Kumar", email = "ayush438500@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pendulum>=3.2.0",
    "typer>=0.27.0",
]

[project.scripts]
fly = "fly.main:app"

[build-system]
requires = ["uv_build>=0.11.29,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
